#include <iomanip>
#include <iostream>

using namespace std;

int main() {
    int numeros[5];

    int numeros2[] = {2, 5, 7, 8};
    // int numeros[4];
    // int numeros[0] = 2;
    // int numeros[1] = 5;
    // int numeros[2] = 7;
    // int numeros[3] = 8;

    for (int i = 0; i < 4; i++) {
        cout << numeros2[i] << endl;
    }
}